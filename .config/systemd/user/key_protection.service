[Unit]
Description=Ensure keys are protected
DefaultDependencies=no
Before=shutdown.target

[Service]
Type=oneshot
ExecStart=/usr/bin/bash -ic 'rm $HOME/.ssh/id_bri $HOME/.ssh/id_cjl'

[Install]
WantedBy=shutdown.target
